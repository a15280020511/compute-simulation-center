# 文献证据运行面

此目录只负责生成冻结文献证据包，不调用数值 Dispatcher。网络仅允许 `api.openalex.org` 与 `api.crossref.org`。

流水线：研究问题结构化 → OpenAlex 检索 → DOI 去重 → Crossref 核验 → 更新/撤稿检查 → 可比性筛选 → 候选参数/先验 → 冻结证据包。

禁止把论文数值自动写入正式参数。必须区分文献原始结果、候选参数、候选先验和正式校准参数。
