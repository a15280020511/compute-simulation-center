# 缺失数据治理规范

1. 先区分 MCAR、MAR、MNAR、结构性不存在、采集失败、时间错位和对抗性隐瞒。
2. 原始观测永远不可覆盖；每个插补值必须保留方法、轮次、区间和缺失原因。
3. MICE 只在相应条件随机缺失假设可辩护时使用。
4. MNAR 必须执行机制情景分析，不得靠单一插补数据正式发布。
5. 完整案例分析必须同时报告样本损失和选择偏差检查。
6. 插补敏感性较高时，结果状态不得高于 `DECISION_CONDITIONAL`。
7. 数据补采优先级由缺失率、敏感性、决策影响和采集成本共同决定。
