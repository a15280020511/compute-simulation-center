# 信息传播与观点动力学规范

1. API 中心或 GPTs 负责文本、账号、时间、地域和关系边结构化；计算中心不抓取数据。
2. 正式任务至少使用多个随机种子，并比较网络结构或传播模型。
3. 结果必须报告分布、网络敏感性、初始节点敏感性和干预时机敏感性。
4. 单次轨迹不得解释为未来真实传播路径。
5. 网络节点和边必须经过合法授权与隐私处理；禁止用于个体账号定向操纵。
6. NDlib 仅作为固定方法后端，票据不能选择任意模型类或导入代码。
