# 系统动力学使用规范

1. 所有库存、流量、单位、时间步长、延迟和容量必须显式登记。
2. 强制执行库存非负、守恒关系、时间步长敏感性和初值敏感性。
3. 长期外推必须检查稳态、失稳、路径依赖、反弹效应和政策退出。
4. 复杂反馈模型必须与简单趋势、无反馈和不干预基线比较。
5. 导入 Vensim 或 XMILE 只允许通过未来受控 PySD 适配器，逐模型做方程一致性验收。
6. 计算结果属于条件情景，不是对长期社会系统的确定性预测。
